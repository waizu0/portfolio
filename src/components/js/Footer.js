function Footer()
{
    
    function clickHandler()
    {
        window.location.href = '../../../docs/Resume.pdf'
    }
    
    return(
        <div class="my-work">
            <h1>This site was created by waizudev using HTML, CSS, and React.</h1>
        </div>

    );
}

export default Footer;
